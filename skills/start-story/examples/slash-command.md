# Start Story

Run the **start-story** skill (`~/.cursor/skills/start-story/SKILL.md`).

**Work item:** ID/URL from this message, else the current branch `story/<id>-...`, else ask.

Optional flags: `--from <step>` | `--pr-only`
