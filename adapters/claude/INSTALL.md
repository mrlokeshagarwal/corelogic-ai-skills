# Claude Code and Claude Desktop

Copy or symlink `skills/dotnet-upgrade` to:

```text
<target-repository>/.claude/skills/dotnet-upgrade
```

Claude Code can then discover the skill within the repository. For Claude Desktop, configure an approved filesystem or development connector that exposes the selected local repository; the skill itself does not grant filesystem or shell access.
