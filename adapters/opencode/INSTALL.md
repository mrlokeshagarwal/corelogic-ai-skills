# OpenCode

Copy or symlink the canonical folder to:

```text
<target-repository>/.opencode/skills/dotnet-upgrade
```

Alternatively use a compatible configured skill path. Keep the canonical content unchanged so updates can be reapplied cleanly.
