## .NET upgrade workflow

For .NET framework, SDK, NuGet, vulnerability, modernization, or upgrade work, read and follow `.agents/skills/dotnet-upgrade/SKILL.md`.

The workflow has a mandatory approval gate: assess and propose first, then stop. Do not modify repository files until the user explicitly approves the target and scope.
