# Architecture

`skills/` is the source of truth. Platform folders are generated or copied views, not independently maintained forks. Every modifying workflow must declare safety boundaries and produce evidence. The first skill, `dotnet-upgrade`, separates assessment from execution through a mandatory user-approval gate.
